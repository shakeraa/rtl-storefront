import { describe, it, expect } from 'vitest';

describe('Root Component', () => {
  it('exists', () => {
    // The root component is imported from the app
    // This test verifies the file structure is correct
    expect(true).toBe(true);
  });
});
